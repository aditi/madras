# Madras markup language

This monorepo contains the toolchain for Madras markup language: a lisp-inspired, pluggable markup language.

## Developers

Use `pnpm` to install dependencies.

```Shell
pnpm --recursive install
```
